name: CI

on:
  pull_request:
  push:
    branches: [main]
    tags: ['**']

# The workflow reads the repository and nothing else.
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    # The cap exists to catch a genuinely hung leg, with headroom for the
    # suite to grow through the rest of the rebuild stack — every time it has
    # trailed the suite, timeouts were misread as flakes. History: 15 minutes
    # fell to R5b's audit pass (six leg cancels, one AT [100%] after 15m16s);
    # 25 fell to pr/03's review rounds (3.13 finished at 24m38s while 3.12
    # and 3.14 were cancelled at 25m on four consecutive runs); 45 fell to
    # pr/07's (3.12 cancelled at 45m while its siblings passed at 37-38m).
    # 75 fell to pr/15's (both legs cancelled at 75m after #79's run had
    # already reached 1h14m). A cap the healthy suite exceeds enforces
    # nothing; 110 stands ~45% above the last green legs, so a hang still
    # cancels while growth through pr/16 fits.
    timeout-minutes: 110
    strategy:
      matrix:
        # 3.12 is the supported floor and 3.14 the newest supported; 3.13 was
        # dropped 2026-08-29 (Tyrel, in session) — it is bracketed by the other
        # two and no failure in the pr/ queue's runs was unique to it, so the
        # middle leg bought compute, not detection.
        python-version: ['3.12', '3.14']
    steps:
      # v6.1.0; full commit pin prevents a mutable tag changing executed code.
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          # The ingress check examines every reachable commit so that deleting a
          # leaked credential in a later commit cannot turn the check green.
          fetch-depth: 0
          # Repository-controlled checks need no authenticated Git operation.
          # Do not leave GITHUB_TOKEN in local Git config while they execute.
          persist-credentials: false

      - name: Set up Python
        # v6.3.0; update deliberately after verifying the upstream release commit.
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: ${{ matrix.python-version }}

      - name: Repository ingress
        shell: bash
        run: |
          printf '%s\0%s\0' "$GITHUB_REF" "$GITHUB_HEAD_REF" |
            python3 .githooks/check_ingress.py --ref-fields
          python3 .githooks/check_ingress.py --history HEAD
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            python3 .githooks/check_ingress.py --ref-object "$GITHUB_REF"
          fi

      - name: Install the frozen dependency environment
        # Two commands because they check different things. `uv lock --check`
        # is the one that fails when uv.lock no longer describes pyproject.toml
        # — `--frozen` deliberately skips that check, so a step with only the
        # sync would install happily from a stale lock and report a green gate.
        # The sync is then the smoke test that the exact `--frozen` install spec
        # 04 assumes really does resolve.
        run: |
          python -m pip install uv==0.12.1
          uv lock --check
          uv sync --frozen --group test --group audit

      - name: Repository checks
        # `--parallel` hands the suite to four xdist workers, one file at a
        # time per worker. It is passed here and not in the local gate: the
        # runner is a fresh machine with nothing else on it, while a laptop
        # gate shares its cores with the session that started it. The census
        # is identical either way; only the wall time differs, and the leg's
        # timeout above is the budget this is meant to keep clear of.
        run: sh .githooks/check-all.sh --ci --parallel

  # Branch protection requires the single context `check`. The matrix above
  # publishes one context per Python version (`test (3.12)` and so on), so this
  # summary job is the one required name, and it refuses unless every matrix
  # leg succeeded — a skipped or cancelled leg is a failure here, not a pass.
  check:
    needs: test
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Every matrix leg succeeded
        run: test "${{ needs.test.result }}" = "success"

  # main never holds an unsterilized instrument: a pull request may carry
  # cleanroom/ drafts while under review, but it may not merge until the tray
  # is empty. Kept as its own job so a loaded tray is distinguishable from a
  # failing check. Make it a required check on main for it to actually gate.
  cleanroom-empty:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      # Keep this pin identical to the checkout used by the check job.
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          persist-credentials: false
      - name: The cleanroom is empty
        # git's own failure must not read as an empty tray: capture ls-files
        # separately and fail on it, then filter. A check that cannot run is
        # a failure, not a pass.
        shell: bash
        run: |
          # NUL-delimited throughout. A path containing a newline would
          # otherwise split into two, and each half would fail to match the
          # README exemption — or a crafted name could match it and hide the
          # rest of the tray. Converting back to newlines for grep would
          # reintroduce exactly that, so each entry is compared whole.
          # A tray check a filename can fool is not a check.
          list="$RUNNER_TEMP/cleanroom-list"
          if ! git ls-files -z cleanroom/ > "$list"; then
            echo "git ls-files failed" >&2; exit 2
          fi
          stray_count=0
          while IFS= read -r -d '' entry; do
            [ -z "$entry" ] && continue
            # **The exemption is by path, so it is checked against the file too.**
            # A pull request can track `cleanroom/README.md` as a symlink: the loop
            # then saw only the exempt name, counted nothing, and the merge gate
            # reported an empty bench while a link stood there for a later reader to
            # follow. `-h` before `-f`, because `-f` follows the link and would call
            # it a regular file. Found by CodeRabbit on pull request 15.
            if [ "$entry" = "cleanroom/README.md" ]; then
              if [ -h "$entry" ] || [ ! -f "$entry" ]; then
                echo "cleanroom/README.md is a symlink or not a regular file." >&2
                echo "The exemption is for the bench's own README, not a link." >&2
                exit 1
              fi
              continue
            fi
            stray_count=$((stray_count + 1))
          done < "$list"
          if [ "$stray_count" -ne 0 ]; then
            echo "The cleanroom still holds $stray_count unsterilized draft(s)." >&2
            echo "Run 'git ls-files cleanroom/' locally to inspect them." >&2
            echo "Sterilize and place them, or remove them, before merging." >&2
            exit 1
          fi
