pytest==9.1.1
pytest-xdist==3.8.0
ruff==0.15.4
# The gate's own suite parses .github/workflows/ci.yml with a real YAML
# parser rather than by searching the file's text, so what it reports is
# what the workflow executes (.githooks/test_r0_contract_ci_matrix.py).
# Declared here because the chamber image and everyday gate install this file;
# it was otherwise present only as a transitive of huggingface_hub.
PyYAML==6.0.3
shellcheck-py==0.11.0.1
huggingface_hub==1.26.0
pillow==12.3.0
pillow-heif==1.5.0
pypdfium2==5.12.1
rapidfuzz==3.14.5
uniseg==0.10.1
boto3==1.43.53
pip-audit==2.10.1
