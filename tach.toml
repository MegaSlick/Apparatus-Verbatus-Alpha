# Shared-module boundaries.
#
# Tach can inspect the importable `common` package. The numbered stage directories
# are not Python packages and are not represented here; their first code must add
# an executable boundary test instead of treating this declaration as proof that
# no stage imports another.

[[modules]]
path = "common"
depends_on = []
